# secretos — nunca subir
.env

# python
__pycache__/
*.pyc
.venv/
venv/

# scratch / inventarios de la herramienta reejecutable
superficie.*.json
*.log

# estado de KiroCrew si se monta como bind mount (NUNCA subir: chats, credenciales)
kirocrew-home/
.kiro/
data/
